/*
Complete the function which converts hex number (given as a string) to a decimal number.
*/

// My solution: 
const hexToDec = (hexString) => parseInt(hexString, 16);


// Takeways: Remember the second argument for parseInt!